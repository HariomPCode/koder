const {
  SUPPORTED_LANGUAGES,
  LANGUAGE_DISPLAY_NAMES,
  isSupportedLanguage,
  normalizeLanguage,
} = require("@koder/shared");

module.exports = {
  SUPPORTED_LANGUAGES,
  LANGUAGE_DISPLAY_NAMES,
  isSupportedLanguage,
  normalizeLanguage,
};

